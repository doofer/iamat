{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Interval list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Startint</th>
                <th>Endint</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('interval_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{% if entity.startInt %}{{ entity.startInt|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{% if entity.endInt %}{{ entity.endInt|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('interval_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('interval_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('interval_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
