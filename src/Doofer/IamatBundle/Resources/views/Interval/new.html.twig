{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Interval creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('interval') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
